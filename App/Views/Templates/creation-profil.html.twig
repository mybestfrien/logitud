{% extends "base.html.twig" %}
{% block main %}
    <div class="container-fluid align-content-center d-flex block-txt flex-column pos-form-1">
        <p>Liste des profils existants</p>
        <form class="d-flex flex-column">
            <label>
                <input type="button" name="prof1" value="ASVP : ex-pervenches">
            </label>
            <label>
                <input type="button" name="prof1" value="Open: conjoints / enfants">
            </label>
            <label>
                <input type="button" name="prof1" value="Gardes-champêtres">
            </label>
        </form>
    </div>
    <div class="container-fluid fond d-flex flex-column">
        <div class="container-fluid fond">
            <div class="align-content-center d-flex flex-column pos-form">
                <form class="d-flex flex-column">
                    <p>Nouveau profil :</p>
                    <label>
                        <input type="text" name="newProf" placeholder="Entre le nom de ton profil">
                    </label>
                </form>
            </div>
            <div class="container-fluid d-flex justify-content-between block-btn">
                <button><a href="http://localhost:8000/profil">Retour</a></button>
                <form method="post">
                    <label>
                        <input type="submit" value="Valider">
                    </label>
                </form>
            </div>
        </div>
    </div>
{% endblock %}